# github-stat-service
Backend service for pulling github stats (stars, forks, open issues, last update, language, etc.). Built with GoLang, pulls user info using GitHub API, stores what it finds in PostgreSQL, and a small REST API to query the data.

